@page
@model CisternasGAMC.Pages.Admin.Cruds.OtbCrud.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Otb[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Otb[0].FamilyCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Otb[0].District)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Otb[0].LastUpdate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Otb) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FamilyCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.District)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OtbId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.OtbId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.OtbId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
